div.container-small

  h2.mt0.mb2.regular.mid-gray Create an organization
  div.bg-white.p2.rounded
  
    div(ng-init="step = 1", ng-show="step == 1")
      h3.mt0.regular.gray Choose a session first
      div.mxn2.clearfix
        div(ng-repeat="session in vm.sessions").sm-col.sm-col-3.px2.relative
          div(ng-show="session.userHasOrganization", style="opacity: 0.8; cursor: no-drop").absolute.top-0.left-0.right-0.bottom-0.bg-white.ml2.mr2.z2
          div(ng-click="vm.formData.session = session",
              ng-class="vm.formData.session == session ? 'bg-blue white' : 'bg-lighter-gray'").block.rounded.p2.relative
            img(ng-src="{{session.avatar}}").rounded.full-width
            | {{session.name}}

      div.mt2
        button(ng-disabled="vm.formData.session == null", ng-click="step = 2").button Next
        
    div(ng-show="step == 2")
      h2.mt0 Choose a name
      label Name
      input(type="text", placeholder="Name of your organization", ng-model="vm.formData.name").sm-col-4.block.field-light.mb2
      p.h5 Great organization names are short and memorable. Need inspiration ? How about <b>mobylette</b>
      label Description (Optional)
      input(type="text", placeholder="Quick description").sm-col-8.block.field-light.mb2
      div.mt2
        button.button(ng-disabled="vm.formData.session == null", ng-click="vm.create(vm.formData)") Create
        | 
        button.button.bg-gray(ng-click="step = step - 1") Cancel
      
