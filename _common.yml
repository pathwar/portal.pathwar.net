portal:
  build: .
  ports:
  - 8090
  - 35729
  #links:
  #- "mock:api"
  volumes:
  - .:/usr/src/app/
  environment:
  - BASE_PATH
  - API_ENDPOINT
  - NO_PUSHSTATE

mock:
  image: pathwar/api.pathwar.net-mock
  #links:
  #- "mongo:mongo"
  ports:
  - 5000

mongo:
  image: mongo
  command: --smallfiles
